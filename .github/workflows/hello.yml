name: Hello Workflow

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from GitHub Actions!"
      - run: date
      - run: echo "Branch: ${{ github.ref_name }}"
